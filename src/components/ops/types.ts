/**
 * Shared types for Ops Dashboard components
 */

export interface SharedOpsProps {
  accessibilityMode: boolean;
  panelClasses: string;
  headingClasses: string;
  labelClasses: string;
  inputClasses: string;
  buttonClasses: string;
}
